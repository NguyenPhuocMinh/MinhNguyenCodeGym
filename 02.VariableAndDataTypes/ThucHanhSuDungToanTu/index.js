var width;
var height;
var width = parseInt(prompt("Enter the width"));
var height = parseInt(prompt("Enter the height"));
var area = width * height;
function showDienTichHinhChuNhat() {
    document.write("The area is:" + area)
}